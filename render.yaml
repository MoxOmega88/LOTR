services:
  - type: web
    name: lotr-ml-explorer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd web-app && python api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
